server:
  port: 8080
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/weroster?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true